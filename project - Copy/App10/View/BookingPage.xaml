<Page
    x:Class="App10.View.BookingPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:App10.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:locad="using:App10.ViewModel"
    xmlns:interactivity="using:Microsoft.Xaml.Interactivity"
    xmlns:core="using:Microsoft.Xaml.Interactions.Core"
    mc:Ignorable="d">

    <Page.DataContext>
        <locad:BookingVM/>
    </Page.DataContext>
    
    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Button x:Name="button1" HorizontalAlignment="Left" Content="Book" Height="80" Margin="785,85,0,0" VerticalAlignment="Top" Width="296">
            <interactivity:Interaction.Behaviors>
                <core:EventTriggerBehavior EventName="Click">
                    <core:InvokeCommandAction Command="{Binding AddBookingCommand}"/>
                </core:EventTriggerBehavior>
            </interactivity:Interaction.Behaviors>
        </Button>

        <ComboBox x:Name="comboBox" ItemsSource="{Binding Numbers}" SelectedIndex="{Binding HowManyPpl, Mode = TwoWay}" SelectedValue="{Binding HowManyPpl, Mode = TwoWay}" HorizontalAlignment="Left" Height="35" Margin="785,182,0,0" VerticalAlignment="Top" Width="130">
            <ComboBoxItem >1</ComboBoxItem>
            <ComboBoxItem >2</ComboBoxItem>
            <ComboBoxItem >3</ComboBoxItem>
            <ComboBoxItem >4</ComboBoxItem>
            </ComboBox>
            <ListView Margin="120,238,0,0" Opacity ="100"  ItemsSource="{Binding Books}" SelectedIndex="{Binding SelectedIndex, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" HorizontalAlignment="Left" VerticalAlignment="Top" Width="314" Height="284" RenderTransformOrigin="0.498,0.529" />
        <DatePicker HorizontalAlignment="Left" Date="{Binding CzechzIn, Mode=TwoWay}"  Height="35" Margin="785,265,0,0" VerticalAlignment="Top" Width="265"/>
        <DatePicker HorizontalAlignment="Left" Date="{Binding CzechOut, Mode= TwoWay}" Height="35" Margin="785,328,0,0" VerticalAlignment="Top" Width="286"/>
        <CheckBox x:Name="checkBox" Content="AllInclusive" HorizontalAlignment="Left" IsChecked="{Binding IsAllInc, Mode=TwoWay}" Height="30" Margin="785,450,0,0" VerticalAlignment="Top" Width="271"/>
        <Button x:Name="button" Content="My Bookings" HorizontalAlignment="Left" Height="80" Margin="120,85,0,0" VerticalAlignment="Top" Width="250" Click="button_Click"/>
        <CheckBox x:Name="checkBox1" Content="Confirm" IsChecked="{Binding IsConfirmed, Mode=TwoWay}" HorizontalAlignment="Left" Height="0" Margin="785,390,0,0" VerticalAlignment="Top" Width="271"/>
        <TextBlock x:Name="textBlock" HorizontalAlignment="Left" Height="45" Margin="595,85,0,0" TextWrapping="Wrap" Text="{Binding Cost, Mode = TwoWay}" VerticalAlignment="Top" Width="190"/>
        <TextBox x:Name="textBox" HorizontalAlignment="Left" Height="35" Margin="960,182,0,0" TextWrapping="Wrap" Text="{Binding CreditCard, Mode=TwoWay}" VerticalAlignment="Top" Width="121"/>
        <TextBlock x:Name="textBlock1" HorizontalAlignment="Left" Height="35" Opacity="{Binding Success, Mode =TwoWay}" Margin="595,130,0,0" TextWrapping="Wrap" Text="Success!" VerticalAlignment="Top" Width="185"/>

    </Grid>
</Page>
